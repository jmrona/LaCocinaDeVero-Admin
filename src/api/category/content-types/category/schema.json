{
  "kind": "collectionType",
  "collectionName": "cms_categories",
  "info": {
    "singularName": "category",
    "pluralName": "categories",
    "displayName": "Category",
    "description": "Menu categories AND days-of-week entries used for the daily menu feature. Lives in cms_categories; migrated once from legacy public.categories, which is kept untouched as a backup."
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {
    "i18n": {
      "localized": false
    }
  },
  "attributes": {
    "category_id": {
      "type": "integer",
      "unique": true,
      "required": true
    },
    "display_name": {
      "type": "string",
      "description": "Nombre en español. Es lo que se ve al asignar categorías a un plato. Se rellena automáticamente desde `name`."
    },
    "name": {
      "type": "json",
      "required": true
    },
    "icon": {
      "type": "string"
    },
    "dishes": {
      "type": "relation",
      "relation": "manyToMany",
      "target": "api::dish.dish",
      "mappedBy": "categories"
    }
  }
}
